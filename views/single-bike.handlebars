<section>
    {{>bike-info bike }}

    {{#if loggedIn}}
        <form class="comment-form">
            <div>
                <textarea class="ml-5" name="comment-body" placeholder="Write a comment"></textarea>
            </div>
            <div>
                <button class="add-button btn btn-outline-dark" type="submit">Add comment</button>
            </div>
        </form>
    {{/if}}

    
    {{!-add items}}
</section>

{{#if loggedIn}}
        <script src='/javascript/add-item.js'></script>
    {{/if}}
