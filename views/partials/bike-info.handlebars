<div class='bike-card'>
  <div id='est_title'>{{brand}} <b>{{model}}</b></div>
  {{#if this}}
    <img src='/images/bikes/{{filename}}' class='img-thumbnail img img-responsive' />
    <span>Price: {{price}}</span>
  {{/if}}
</div>