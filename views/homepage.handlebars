<h1>Reviews</h1>
<ul class="reviews">
    {{with as |post|}}
    <li>
        ${new Date().getFullYear()}

        <div class="meta">
            {{part_comment.user}} {{bicycle_comment.user.username}} on
            {{part_comment.created_at}} by {{user_name}}
            |
            <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
        </div>
    </li>
    {{/with}}
</ul>

<script src="moment.js"></script>